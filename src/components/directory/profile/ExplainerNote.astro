---
interface Props {
  title: string;
  body: string;
}

const { title, body } = Astro.props;
---

<details class="explainer-note text-brand-gray-600 text-xs">
  <summary class="text-brand-blue cursor-pointer font-semibold">{title}</summary>
  <p class="mt-2 leading-relaxed">{body}</p>
</details>
